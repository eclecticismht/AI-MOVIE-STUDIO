function saveFaceRefineMode(value){if(!['auto','on','off'].includes(value))return;const p=activeProject();try{localStorage.setItem('aimovie_data',JSON.stringify({...D,projects:D.projects.map(x=>x===p?{...p,faceRefineMode:value}:x)}));p.faceRefineMode=value;document.getElementById('faceRefineSettingStatus').textContent='已保存，之后创建的生成任务使用此设置。'}catch{document.getElementById('faceRefineSettingStatus').textContent='保存失败，请重试。'}}
const faceReviewRender=renderReview;
renderReview=function(){faceReviewRender();for(const job of items('jobs').filter(j=>j.faceRefine)){const card=document.createElement('div');card.className='card';card.innerHTML=`<b>${esc(job.shot)} · 人脸精修</b><p>${esc(job.faceRefine.reason||job.faceRefine.status)}</p>${job.originalVideoUrl?`<a href="${esc(job.originalVideoUrl)}" target="_blank" rel="noopener">查看精修前原视频</a>`:''}`;document.getElementById('review').append(card)}};
faceRefineControls=function(){return `<div class="card"><h2>人脸自动精修 · Face Refine</h2><label>生成后处理<select aria-label="人脸精修模式" onchange="saveFaceRefineMode(this.value)">${[['auto','自动：人脸偏小或模糊时精修'],['on','开启：检测到稳定单人脸就精修'],['off','关闭：直接使用原视频']].map(([value,label])=>`<option value="${value}" ${(activeProject().faceRefineMode||'auto')===value?'selected':''}>${label}</option>`).join('')}</select></label><p id="faceRefineSettingStatus" role="status"></p><p class="muted">抽查 5 帧，单人脸稳定出现且多数帧小于 96 像素或清晰度偏低时自动精修。多人、背影、无可靠人脸或缺少唯一角色参考图时跳过。精修额外占用渲染时间，原视频和原声保留，结果仍需审片。检测或精修不可用时回退原视频并显示原因。</p><details><summary>多人镜头：手动选脸工具</summary><a class="btn" href="/workflows/H3-FaceRefine-Manual.json" download>下载手动选脸工作流</a><p>在 ComfyUI 中加载本镜原视频与角色参考图，再选择要修复的人脸。</p></details></div>`};
