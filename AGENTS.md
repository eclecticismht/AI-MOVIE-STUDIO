# 项目协作约定

- 用户要求：每次完成代码修改并通过相关检查后，创建 Git 提交并推送到本仓库 GitHub 远端。无需重复询问同步许可。
- 同步前检查变更范围，排除密钥、日志、运行数据、上传素材和生成媒体；不要使用强制推送覆盖远端历史。
- 运行 `run-unit-tests.ps1` 验证相关功能；同步失败时明确告知用户，不得声称已同步。
- 在最终回复中简要注明同步结果和提交编号。
