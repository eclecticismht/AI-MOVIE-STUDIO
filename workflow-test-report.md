# 《老实人》流程试跑

## 完整自动制作（2026-09-17 02:07 启动）

- 完整批次 55 镜，推荐时长合计 475 秒（7 分 55 秒），每镜 4–15 秒。
- 已引用项目资产，采用第 1 版经校对的完整剧本。H3 提示词已由工具按批自动编写并缓存。
- 任务 ID：film_90093f35d05903c5。状态查看 `/api/film`，制作计划和后续输出位于 `film-runs/film_90093f35d05903c5/`。
- 第一镜已进入真实采样，尚未完成全片。自动逐镜渲染、合成、添加字幕，导出 720p MP4；生成音轨保留，不声称实现了独立 TTS 或声音克隆。
- 已用三个原有试镜实际验证拼接、中文字幕及有声 MP4 导出，验证文件约 15.57 秒，仅为合成器测试，不是完整故事成片。
- 已创建每 30 分钟跟进的自动化，截止北京时间 2026-09-17 07:00。完成后提前暂停；问题授权自行判断处理。

## 故事 → 剧本与资产

- 使用用户完整故事，故事页与剧本页共用原文。
- 修复故事页只有梗概、没有完整原文及下一步入口的问题。
- 真实调用 DeepSeek 成功，第 2 版剧本已保存，保留第 1 版。
- 自动建立 11 个角色、6 个场景、13 个道具，项目内可编辑。
- 第二次 500 元没有转出，结尾余额 603.72，手机震动两次不翻看，核心结局正确。
- 新生成稿存在内容问题：亲属归属错误、同一工地重复成场景、人物头像混淆、语音秒数不合理、未明确写出 12:58 收款。
- 已在角色库把错误的“孙嘉俊外甥/媳妇”更正为“老张的外甥/老张外甥的媳妇”。正文中的同类错误仍需校对，不能将生成成功等同于内容审核通过。
- 已加强后续生成规则：核对亲属归属、场景别名合并、闪回出场分类、时间及语音长度。

## 渲染

- 延续此前已审核并入队的三个 5 秒镜头：工地环境、陈实反应、结尾手机震动。
- 这三个镜头来源于第 1 版已校对剧本，不冒充第 2 版重新生成的分镜。
- ComfyUI 恢复后已真实提交三项任务，未使用模拟完成。
- 已用 ComfyUI 实时事件替换虚构进度。现场验证第三镜头从 16/25、64% 更新至 20/25、80%，显示采样预计剩余 39 秒；25/25 后等待解码保存，不提前宣布成片完成。
- 分镜上方已增加 H3 生成尺寸，校验 32 的倍数，入队时保存快照；现场验证非法 1920×1080 被拒绝，736×1280 能保存，最后恢复 864×480。
- 实际生成规格为 864×480、24fps、124 帧，不是项目标注的最终 4K。
- 三个镜头均已真实完成并回流审片页，每条 124 帧（约 5.17 秒），均包含音轨。后两条各耗时约 3 分 32 秒。
- 抽帧检查：工地环境可用；第二镜头没有完全遵守“手机在画面之外”的构图要求；第三镜头把独居出租屋生成成上下铺宿舍，人物一致性也未通过参考图约束。三个视频均保留“待审核”，未选为 MASTER。仅抽帧检查，未将音轨存在等同于声音质量合格。

## 本地渲染服务启动副作用

已安装的 ComfyUI 在启动时自动将资产目录数据库从 0006 升级到 0007。日志提示重建目录并丢弃旧手工标签、元数据、预览、重命名及任务关联，升级前备份保存在 `C:\AI\Comfy UI\ComfyUI\user\comfyui.db.bkp`。此目录独立于工作室浏览器中的剧本、资产及项目数据。


## 2026-09-17：真实资产图片引用修复

- 原问题：资产只有文字引用，队列丢弃媒体输入，所有渲染使用 FL2VA 的 t2v 路径。
- 修复：浏览器按当前分镜收集角色/场景/道具图片，上传本机 ComfyUI 并保存内容哈希快照；普通入队和自动成片计划都传递同一 references 结构。
- 渲染：Ref2VA 专用 int8 模型，LoadImage → MiniMaxH3DirectorGroupReferenceToVideo → Director.r2v_groups；Picture N 和 Subject N 顺序固定，每镜最多 9 图。专用模型已在本机安装。
- 缺图、失效图、跨项目资产、超过上限均阻止提交，不退回纯文字模式；纯空镜未引用任何资产时保留 T2VA。
- 旧队列在首次提交时补参考图；已经被连接器接受的旧无图任务会被识别并要求重新入队，避免继续无图生成。已完成成片保留。
- 51 项自动测试通过，涵盖普通队列、模型/图像连线、成片计划、缺图拦截、上传格式、重复点击和切换项目。
- 实机单图验证：REF_TEST_20260917_01，Comfy prompt ba0f0772-35ff-45d1-b6fd-0ef041580963，864×480、有音轨，成功生成。参考图与输出对比保存在 test-artifacts/reference-render-check.jpg，执行证据 reference-render-history.json。
- 此单图测试发现外部 Group 默认向上对齐帧数，已改为传入与主流程相同的对齐后秒数，避免自动字幕/拼接时间线偏移。
- 双图自动成片测试：film_dd9a53000c5bcdfa（角色+场景），独立测试项目，不覆盖老实人原片。
- 页面读取确认现有角色库只有文字、没有图片；分镜已明确显示缺图资产。完整重制按钮测试被自动审批拦截（可能触发整片 GPU 渲染），未绕过该限制。独立短片用于验证成片路径。
- 限制：引用图片不能保证绝对外观一致；视频/音频参考尚未接入。原项目需要正式参考图片才能按新规则生成。

- 双图成片验证已完成：2026-09-17 10:03，北京时间；1280×720，90 帧全部解码成功，含 AAC 音轨，总容器时长 3.797 秒（4 秒请求按 H3 网格对齐为 90 帧）。证明参考图片随成片计划进入渲染，并完成自动合成。


## 通用对白管线与系统预检
- 删除 screenplay-api 中人物、地域、房间、结尾的故事硬编码，取消固定运镜覆盖分镜。
- 新增 dialogue-contract.js，共享解析 speech/screen/sound，绑定 speakerId/name/delivery；对话字句独立于模型视觉改写。
- 预检无 GPU/模型调用；检查源文台词、显式说话人物、时长、多个发声人物、图片缺失和旧提示词。
- 浏览器实测原 55 镜识别 61 项问题，其中第 21、25 镜错误地把“他打字”作为陈实口头对白；另四镜含多人发声。
- 实测通用重建按钮校正 6 个原镜头，创建 63 镜新批次，原批次保留且未渲染。
- 资产包导入实测：4 张生成角色参考图已按名称匹配导入 AMS-003；其余场景、道具图片尚未补齐，遵循用户“工具调试优先”的最新要求，未继续批量制片。
- 字幕排除人物标签和环境音说明；实际语音并未做 ASR 逐字验收，不声称已解决所有模型说错词或口型问题。

## 2026-09-17 资产补齐与生成后检查
- 已更新 /assets/laoshiren-v2/manifest.json，浏览器导入成功 23 项（4 角色、6 场景、13 道具）。63 镜校正版生成前检查 0 项问题；这不是成片质量验收。
- 仅提及角色不再被要求提供视觉参考图，也不写进默认视觉角色描述。
- 创可贴第一版错误画到拇指，未采用；改为独立道具参考。朋友圈自拍重新绑定孙嘉俊脸部参考。
- 本地 faster-whisper small 模型和依赖放在 .runtime，CPU int8，不发送音轨到外部服务。
- 成片页真实调用检查：film_90093f35d05903c5，55/55 镜完成，识别结果保存在 run.json 的 audit。旧任务缺少结构化对白，均明确为无法自动核对，不伪造通过。
- 示例识别：第 3 镜「邦京直播到周建善待後到」，第 7 镜「老實人 還有多少快了快了是多少兩千來塊吧」，第 8 镜「你什么时候学会搭费所问了」。仅为 ASR 输出，不保证逐字准确。
- 单镜重做创建独立子版本、复用未选镜头、不覆盖原片；已有单元测试，真实 GPU 重做仍待下一阶段。失败任务后续恢复使用新尝试 ID。
- 65 项自动测试通过；页面验证检查结果显示完成 55/55。新 63 镜未进行完整渲染。
- 下一阶段必做项见 DEVELOPMENT-NEXT.md：通用道具状态/精确屏幕文字、三类短镜头实测、问题镜头修订与重做、完整回归。

## 通用状态与三镜真实回归
- 新增 asset-states.js / asset-states-ui.js：分镜可独立覆盖资产状态、图片和屏幕原文。新分镜 API 返回 assetStates；不允许模型编造图片地址。
- 屏幕文字由 Canvas 生成参考卡，入队时上传替代历史图片；原资产库和已入队快照不变。数字边界校验拒绝用 603.72 匹配 1603.72。参考卡不等于最终视频里的屏幕字已准确。
- 浏览器已保存第 22 镜 1603.72、第 60 镜 603.72，以及第 58 镜 603.72 与再次索款消息；预检仍通过。
- 电话里发声但画外的人物，不再作为画内脸部图片上传或写入默认角色视觉描述。
- 独立任务 film_85cecde483a0e862，三镜各请求 4 秒、864×480，引用陈实、塔吊阴影和手机。已完成渲染与自动合成，旧片未覆盖。
- 实际 ASR：现场「快了」、电话「我不是不還你啊」、静默镜头无识别文字。加入 OpenCC 简繁归一后，三镜均 text_match；speakerIdentity 仍 not_verified。
- 抽帧保存 test-artifacts/qa-onscreen-frame.png、qa-phone-frame.png、qa-silent-frame.png。电话帧中的陈实闭嘴看手机，不以单帧声称全段口型验收完成。
- 71 项自动测试通过，含原文金额、状态图片上传、电话人物排除与简繁差异。单镜 GPU 重做、所有旧分镜状态修订、精确屏幕合成、完整成片复测仍待完成。

## 精确屏幕合成与旧分镜复查
- screen-cards.js 统一验证原文、金额边界、长度和布局容量；成片计划保存卡片快照。
- 自动合成使用 ASS 确定性绘制卡片，内容宽度自适应，不依赖视频模型画出正确汉字。只去掉已被卡片覆盖的重复字幕，其他屏幕信息仍保留。
- 独立重合成任务 film_a3ba445d73f947cd 已完成；继承三镜原视频，clip-2 SHA256 与父任务一致，compositionOnly 标志禁止素材缺失时回退到 GPU 渲染。原片保留。
- 新增“按所选原批次更新文字（不渲染）”入口；跨批次 ID、不在保存原文的文字、缺失素材均拒绝。
- 渲染检查 test-artifacts/screen-composition-frame.png：右上方卡片“微信消息／再转五百呗／我在买单”，不遮挡中间人物脸部。此功能不是透视跟踪手机屏幕。
- 发现第 38 镜朋友圈文字被解析为孙嘉俊口头对白：修复同一行屏幕文字中的人物标签拆分；明确标注配文的原文不能作为口头对白。已新增测试并修正当前分镜。
- 浏览器修正第 15/16/25/29/35/55/58 镜错误现场孙嘉俊引用，第 38 镜增加精确配文卡。63 镜预检通过。
- 下一阶段仍需处理第 53 镜多场景压缩和错误场景引用、回忆年龄服装、剩余信息卡与发声时间对齐，之后再完整重跑。
# 2026-09-17 续开发验收

- 补齐夜间公交、城中村街巷、楼道和两位角色高中校服参考图，通过页面导入/绑定。
- 页面拆镜保存成功，65 镜总计 488 秒；预检 0 项需处理，旧镜头归档保留。
- 82 项自动测试通过，包含拆镜持久化失败保护和字幕音轨对齐。
- 字幕对齐实测子版本 film_31432432082bf266 完成：首句 2.22–3.22 秒、电话台词 5.43–7.43 秒；静默消息 7.50–11.25 秒。复用视频，无 GPU 重渲染。
- 尚待真实回忆/场景视频验收、其余文字状态、修订后单镜重做及完整端到端回归。预检通过不代表成片质量通过。

## 单镜修订闭环实测
film_f2358d1e8ae2ac22 已完成。只重渲染第一镜，第二、三镜原始和规范视频哈希保持一致。新字幕首句2.29–3.29秒，2镜对白对齐无问题。抽帧 test-artifacts/revision-frame.png 验证低头动作与字幕。83项测试通过；旧版保留，新增打开原版对比入口。当前样本新增8处屏幕状态，65镜预检0问题。

## 动态草稿与纯黑尾镜验收
- 资产状态新增 screenEffect=type-delete；每行一段，按字数分配时间，逐字输入、停顿、删除，最终清空；标明未发送。动画文字仍受原文校验。
- 动态卡右下定位并固定尺寸，不随字数跳动；原模型输入使用未发送草稿空白参考，精确文字由合成器处理。当前仍是信息卡，不是手机屏幕跟踪。
- 显式 renderMode=black 由本地合成器直接生成纯黑静音视频，不调用图片上传、提示词模型或视频模型；批量渲染队列跳过该镜，单镜入队给出说明。
- 样本第61镜已配置三段未发送草稿，第65镜已配置纯黑静音。65镜488秒，预检0项问题。
- film_df532d567cf9cd0c 为合成技术测试（复用工地静默样片延长，非故事结尾成片），生成完成，未产生模型任务。抽帧确认完整草稿、清空、黑屏；黑屏源片实测3.75秒，全程静音。85项测试通过。
- 下一阶段：高中校服与公交/街巷/楼道参考的视频实测；其余普通屏幕状态复核；完整故事回归与人物/发声/节奏审片。完整成片尚未验收。
# 2026-09-17 补充：选镜样片与连续性

- 选镜功能：空选明确报错，按原分镜顺序保留时长，限制1–12镜。真实UI只选黑场成功生成 film_19310bbf36dd738f，未发起模型任务。
- 四镜样片 film_2bb3c72caafe3674 已完成；test-artifacts/continuity-inspection.json 记录实际 ComfyUI 执行历史的资产绑定，4张接触表记录抽帧。
- 校徽偏差未解决：仅重做高中镜头的 film_444fae542c59ce8c 仍有额外校徽；更严格提示词确已进入实际执行图。后三镜源片和合成片哈希均保持一致。
- ASR：两版均3镜需复核，1镜无识别对白。高中读标签风险、公交疑似人声、楼道低置信度识别保留为未通过项，不声称音画验收成功。
- 88项测试通过；完整65镜故事尚未重新生成或验收。下一阶段处理造型保持与无对白声音控制。
# 声音控制回归
- film_6223050b938c72eb：四镜仅合成静音，不重新生成画面。原始source哈希均未变。
- 15.04秒最终片音量检测 mean/max -91dB（16位检测静音底），处理后四镜ASR均无识别文字。
- 90项测试通过；验证静音拒绝有对白或未知对白的镜头、父版本不变、切回原模型声音。
- 无对白环境声保留、造型一致性仍待解决，不能将整镜静音称为人声分离。
# 首帧控制回归
- 93项测试通过。首帧路径校验、缺图拒绝、纯黑冲突、I2VA图输入、提示词结构与音频规则均有覆盖。
- film_e7cc121dcdeec577 完成；test-artifacts/first-frame-binding.json 为真实执行历史核对，first-frame-contact.jpg 为4个抽查画面。
- 校服未再出现额外校徽，小幅动作中人物与衣服保持较稳定。保留原始模型音轨的ASR结果为空，并非通过整镜静音消除问题。
- 当前19镜UI保存并重新打开验证首帧地址成功。全片和复杂动作尚未验证；自动生成首帧图片尚未内置工作室。
# 综合修复验收
- 96项测试通过。新增环境音文件校验、只替换无对白镜头、纯黑冲突与首帧发声者必填位置校验。拆场景清理旧首帧/环境音；拆对白清理发声者位置。
- film_18174fa6f8203626：转头与转身，未见额外校徽，ASR为空。
- film_efd8860536c71a68：右侧说“你跟别人借点呗”，ASR原句匹配、字幕对齐，抽查右侧开口左侧安静；实际执行图包含位置与原句。
- film_d239b6b173eaefd2：仅第一镜替换独立教室音轨；mean -47.7dB / max -36.3dB，非静音，ASR为空。四个原始source哈希保持不变。
- 第19镜错误首帧已清除，第30镜绑定教室首帧和右侧发声者，剩余备注/预算/截图屏幕状态已处理。65镜预检0项。
- 未关闭事项详见OPEN-ISSUES.md，不能把本轮结果写成全片验收通过。
