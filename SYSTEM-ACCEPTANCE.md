# AI MOVIE STUDIO 本轮研发验收

## 2026-09-20 完整故事制作与工具修复验收

以下为最新结果；后面的 2026-09-19 记录保留为历史，不代表当前成片状态。

- 最终版本：`film_6c37d0e6b017be7e`。99/99 镜完成，实际 553.33 秒（9 分 13 秒），1280×720，H.264 / AAC，95,171,956 字节。
- 实际 MP4 全量解码通过：13,279 个视频帧、26,047 个音频帧；音视频尾部相差约 0.008 秒；HTTP HEAD 200、分段读取 206。
- 83 个模型镜头的实际 ComfyUI 图片输入与制作计划一致，其他镜头使用本地屏幕/黑屏合成。使用首帧的镜头验证的是首帧输入，不表示模型同时读取所有独立资产图。
- 24 个口头对白镜头字幕已按声音对齐，未使用人工“已试听”放行。核对保留原始转写和独立中文交叉复核记录；识别通过不等于证明声音人物或口型完美。
- 已查看实际合成视频的 99 镜中帧总览，并对修订镜头查看 12 帧联系表。确认关键金额 1603.72→1000 转账→603.72、朋友圈三图、未发送的回复和手机屏幕朝下结尾；第 97 镜残留模型字幕已在最终子版去除。结尾第 98 镜使用本地生成的两次震动音轨。
- 工具修复：批量暂存修订、首帧候选选择、继承首帧编辑崩溃、屏幕原图替换、自动独立语音交叉复核、已核对字幕时间复用、保留比例裁切、真实制作进度与临时检查状态显示。
- 资产上传限制为 5MB，支持 JPG / PNG 与视频格式；真实 MP4 上传和自动封面生成通过。视频目前通过封面参与图片参考，并非视频动作参考条件。
- 224 项隔离自动化测试全部通过。本地服务已重启加载本轮修复，最终完整成片再次验证通过。

证据：`test-artifacts/film_6c37d0e6b017be7e-export-verification.json`、`test-artifacts/film_6c37d0e6b017be7e-reference-bindings.json`、`test-artifacts/film_6c37d0e6b017be7e-visual-review.json`、`test-artifacts/full-story-unit-tests.txt`。

边界：这是完整工作流交付和技术验收，不是逐秒人工试听或电影级视觉一致性认证。生成镜头仍有手机界面文字、服装细节和人物外观的局部变化；准确故事消息使用本地信息卡保证可读。原故事部分生活背景细节在改编中被压缩，不能称为逐字还原。4K 输出和原生视频动作参考仍不在本轮已完成能力中。

---

日期：2026-09-19。结论：本轮工作流可靠性修复和本地合成验收通过；不等于无人值守全片质量或所有规划能力已完成。

## 已完成

- 直接编辑分镜和成片修订回写都会使相关承接镜失效，清除过期缓存。
- 队列提交、结果同步、审片批准、清空队列、MASTER 下游使用校验来源版本。
- 审片删除会清理相关 MASTER；删除标记防止旧结果再次同步复活。
- MASTER 批准先完成持久化再更新内存；同一镜头只保留一个当前批准版本，历史批准结果保留。
- 分镜入队原位反馈、固定左侧导航、修订预检、声音复核与历史记录。
- 设置新增数据备份、恢复预览、恢复保护。制作中不能恢复；存储失败不替换当前内存数据。备份不含会话密钥，磁盘媒体须另行保留。
- 停用模拟完成与仅修改状态的 4K 入口，避免把演示状态当实际能力。
- 本地静态服务拒绝私有路径、目录穿越和畸形编码请求。

## 验证证据

- 186 项自动化测试通过：`test-artifacts/system-acceptance-tests.txt`。
- 独立测试项目真实完成屏幕镜头 + 黑屏的本地合成；视频 HEAD/范围读取、修订预检、声音更新子版均通过。
- 验收版本：`film_e249acada162f82d`；声音更新版：`film_05fd65c5706b34c3`。
- 抽帧确认 1603.72 屏幕内容和随后黑屏，记录：`test-artifacts/system-acceptance-contact.jpg`。
- 真实页面巡检故事、剧本、角色、场景、道具、分镜、AI生成、审片、自动成片和成片；无可见界面错误。备份下载已实际触发，未在用户数据上执行恢复。
- 服务 4173/8080/8188 可访问；GPU 队列为空。未提交新的 H3 渲染或文字模型请求。

## 不能省略的边界

- 现有正式全片 `film_13da8741adfbe5a9` 仍在第 3/66 镜等待声音复核。本轮没有完成或批准该片。
- ASR 只辅助对白文字核对，不能证明说话人物、口型、表演和叙事正确；仍需实际审片。
- 4K 超分辨率尚未接入。当前可用的自动成片输出为 1280×720；不能称为完整 4K 生产系统。
- 旧任务无来源校验记录时不自动信任；新制作记录才支持可靠的来源追踪。
- 本轮未重新验证真实 DeepSeek 调用；服务端配置返回 configured=false，文字生成需在页面配置有效连接。
- 浏览器备份包含数据和媒体地址，不打包磁盘视频、模型文件或全部外部资产。

下一次发布级验收必须包含新的完整故事实际生成、资产一致性、声音及叙事逐镜验收与最终成片验收；不能用单元测试或本地合成样片替代。
