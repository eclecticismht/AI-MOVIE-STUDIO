最新续接先读 [STAGE-TWO-HANDOFF.md](STAGE-TWO-HANDOFF.md)，以下保留历史。

## 2026-09-18：按用户确认的三阶段顺序执行（进行中）

1. 已检查当前66镜、492秒的故事/剧本/对白/资产；移除第12镜新增台词，修订13处混合说话动作，补齐3张本地道具图及12处引用；第65镜承接第64镜。原批次历史sourceContent保留；可编辑源剧本已修正。检查包 production-reviews/review_cf17a6f5a6b6a1c3.json，人工/文字报告 test-artifacts/full-plan-review.json。
2. 关键场景9镜样片 film_b166c83674e2ac8b 正在渲染，覆盖原7/8/17/39/61–65镜。尚未验收。检查已编译提示词又发现环境/道具历史剧情污染，已新增reference-visual-notes通用隔离；前端、首帧和Ref2VA统一只读外观与当前状态，测试通过。此修复尚需在当前任务结束后重启连接服务并重新验证受影响镜头，不修改运行中的历史快照。
3. 关键场景通过后再完整制作、逐镜验收约8分钟影片，用户本轮明确批准这一顺序。尚未启动全片，不通过拆批规避任何自动审查拒绝。

新增本地制作检查包导出及故事原句核对，拆多人对白后的动作复核拦截。完整130测试此前通过，新资产历史隔离14项通过，合计新增1项后待最终全套复跑。

## 2026-09-18：对白动作拆分与生成链路续修（当前）

- 已新增安全两段拆分入口；前段保留原对白，后段无对白并承接尾帧。原镜归档，状态深拷贝、清除旧渲染缓存、显示新增时长；明确确认旧首帧后验证并重建来源记录。已通过真实UI将《老实人》第19镜拆分，当前66镜492秒，制作预检0项。
- 自动分镜提示与解析增加 continuePrevious；仅同场景、机位、资产且无口头对白的后镜可承接，保存时绑定实际新镜头ID。模型生成规则经接口回归验证，本轮未另调用DeepSeek生成整批。
- 修复对白校正重建批次时沿用旧首帧来源和旧承接编号；新批次重新映射，拆多人对白时清理首帧。
- 重写实际使用的镜头提示词编译入口，只取本镜选中的资产与当前状态，不再把角色传记中的工作服等历史信息混入回忆。显式屏幕肖像可覆盖“仅提及”元数据，仍只作为屏幕内容。
- 承接前后必须使用同组资产图片；变化时拦截。首帧单镜修订不允许更换画内发声者后继续沿用旧位置。修订版本记录连带重做镜号；字幕未对齐的具体镜头可直接进入修订。
- 128项自动检查通过。另修复视频HEAD请求返回JSON的兼容错误；实测HEAD返回video/mp4与正确长度，Range返回206及16字节指定片段。
- film_d544fd3fc33dc33e：两段样片完成，真实执行图与图片哈希证实尾帧进入后镜I2VA。前段手留口袋无提前递钱，后段左白袖陈实递给右蓝袖孙嘉俊，后段ASR空。前段ASR将“借”识别为“接”，两种识别设置仍有差异，保留needs_review，不能判定对白通过。
- film_38a8e000fe291b09：经工具retry接口调整说话节奏，实际触发前镜及后镜两镜级联重做；新尾帧哈希与真实执行图一致。逐帧检查前段不递钱、后段陈实左向右递给孙嘉俊，末帧右侧持纸币。增强本地识别两镜均text_match（对白原句一致，后镜为空）。
- 新增已安装并校验的 Whisper medium 本地增强复核。官方权重 SHA256：9b45e1009dcc4ab601eff815b61d80e60ce3fd8c74c1a14f4a282258286b51ae；provenance 记录于 .runtime/models/whisper-medium。下载曾截断，校验阻止启用，分段恢复后完整验证通过。
- film_d92732a3f2f0c2a6：只复用上述两段视频，使用增强识别对齐字幕，1镜已对齐/0项问题；输出1280×720、24fps、282帧、11.75秒、带音轨。两个source文件SHA256与父版本完全一致，未重新渲染。证据见test-artifacts/dialogue-action-alignment-reuse.json和dialogue-action-final-contact.jpg。新制作默认优先已安装medium；普通/增强检查保留历史记录，识别一致也不自动证明人物/口型正确。
- 完整66镜尚未重渲染逐镜验收；短样本不能替代全片。历史段落和旧测试计数以下保留，仅以本节和最新验收补充为准。

## 2026-09-18：连续动作的尾帧承接

- 分镜编辑新增“动作衔接（自动成片）”：后镜可明确引用同批次紧邻前镜的最后一帧。当前限无口头对白后镜，用于对白与后续动作分开制作。
- 服务逐帧解码前镜原始视频，取最后一帧，上传至本地 ComfyUI，作为后镜真实 I2VA 输入；记录来源镜头、任务及图片文件，不复用历史承接图。
- 漏选/错序/缺失前镜、尺寸不同、纯黑衔接、独立首帧冲突会拦截；独立队列明确要求改从自动成片入口制作。
- 前镜重做会连带清除并重做连续依赖链，其余镜头复用，旧版保留。
- 验证：114 项测试通过；已有 864×480 样片实际尾帧提取与反向解码首帧逐像素一致，证据 test-artifacts/continuity-extraction.json。本地服务已重启，首页 HTTP 200。
- 尚未验收：本轮未做新的两阶段模型渲染，不能据此宣称“先说后递钱”的视觉结果已通过。现有拆分入口仍只处理无对白镜头；对白+动作的一键拆分需继续补充原文与事件分配、时长和首帧溯源处理。完整 65 镜成片仍未重新制作验收。

# 当前验收清单（2026-09-17）

## 2026-09-18 续修
- 新增首帧来源检查：采用时记录画面、行动、机位、本镜状态及参考图片；改动后预检/单镜/批量/自动成片拦截旧首帧。实际UI把第19镜机位临时改动，预检准确报1项过期；恢复后65镜0项。历史无来源记录的手动首帧不会被虚构为已核验。
- 换首帧时清空发声位置；改说话人物且未调整原位置时，也清空位置，防止新人物沿用旧人的左右位置。首帧来源指纹对大图片保存紧凑变化标识，避免重复塞入整张图片。
- 新增人物出现方式：现场、仅屏幕/照片、画外。屏幕肖像仍上传为屏幕内容参考，不作为现场人物；出现方式与画内对白冲突时拦截。第38镜孙嘉俊已通过UI设为仅屏幕照片；此为数据/条件绑定修复，尚未完成该镜视频视觉验收。
- film_9ed5045b641cc60b：本地小卖部首帧驱动8秒双人镜头完成。ASR逐字一致，字幕分段对齐；半秒抽帧可见右侧发声、左向右递钱、两套校服区分保留。模型未严格遵守“说完再递钱”的测试约束，动作时序仍待改进，不宣称完全通过。
- 当前累计112项测试通过；整片尚未重新渲染验收。

## 已修复且有实测证据
- 首帧转头/转身校服保持：film_18174fa6f8203626 两个6秒请求完成；逐秒抽帧未见新增校徽，人物服装区别保留，ASR无识别文字。
- 首帧发声者定位：必须明确左/中/右。film_efd8860536c71a68 右侧说“你跟别人借点呗”，ASR原句匹配、字幕对齐，半秒抽帧右侧开口左侧闭口。执行历史核对位置和原句均已输入。
- 独立环境音：支持导入WAV/MP3、保存文件快照、无对白镜头替换模型音轨，不是整镜静音。film_d239b6b173eaefd2 第一镜采用已检查的教室音轨，复查无识别文字，非静音音量，所有原始source哈希未变。其余旧镜头未修改，不宣称整部旧样片通过。
- 拆镜：清理旧首帧和场景环境音；按对白轮次拆镜清理旧发声位置，避免错用。新计划和制作预检检查声音方式。
- 项目数据：第19镜小卖部的错误教室首帧已清除，第30镜教室配置首帧与右侧发声者。第15镜删去无原文依据的文字，第16镜备注、第27镜预算、第23镜截图连续状态已处理。65镜制作前预检0项。
- 可明确清除首帧，避免只清空字段未生效时误用旧图。上传期间切换编辑对象不把文件写到另一个镜头。

## 尚未关闭，不能宣称全部完成
- 本地首帧后端已配置并接入分镜编辑：Krea2 Turbo + Identity Edit v1.2 r128，模型SHA256校验通过。真实UI生成、候选预览、采用、保存已跑通；frame_ac37c97fbbc8f3bd 为公交单人样本，已保存到第53镜。
- 双人流程修复：两张角色肖像经ImageStitch合并，同时与所选场景参与同一次生成；每张原始资产都有实际LoadImage节点。早期分轮结果发生校服混用，未采用。通用页面复测 frame_a28ed9f7a69d166d 保留白袖/蓝袖区分、所选小卖部、无手套和提前递钱，已保存到第19镜，右侧发声。仅此静态样本通过，不代表任意多人物或后续视频均已通过。
- 首帧状态与视频动作分离：默认静态起始画面，不把完整递钱动作送进首帧而造成收款方提前持钱；页面可选填首帧起始状态。后续人物动作仍保留在原分镜供视频生成使用。
- 已定位并修复人物传记污染画面：图片和视频的参考描述均使用本镜状态；没有本镜状态时，人物外观以参考肖像为准。公交前后对照中错误手套已消失；资产原档案和任务快照仍保留。
- 本地图片→首帧→H3→自动成片实测完成：film_d029631336729782，4秒公交样本，真实执行历史中的唯一图像输入与本地首帧哈希相同；半秒抽帧保持灰蓝短袖和裸手，ASR结果为空。65镜制作条件检查0项，但不是整片质量通过。
- 修正后的65镜整片尚未重新渲染和逐镜验收。短镜头验证不能替代全片。
- 模型身份/动作/对白不能由一次样片证明普遍正确；当前通过项只针对所列样本。仍需整片审片与失败重做。

测试：106项通过。本地首帧任务及逐轮工作流证据见frame-runs，图像见assets/generated-frames；操作、视频任务ID、抽帧和音频证据见test-artifacts及film-runs。旧开发记录按历史保留；本文件优先说明当前状态。
