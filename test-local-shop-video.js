const fs=require('fs'),path=require('path');
const {uploadReference}=require('./reference-assets');
(async()=>{
 const frame=JSON.parse(fs.readFileSync('frame-runs/frame_a28ed9f7a69d166d.json'));
 const firstFrame=await uploadReference('data:image/png;base64,'+fs.readFileSync(path.join(__dirname,frame.imageUrl)).toString('base64'),'http://127.0.0.1:8188');firstFrame.speakerPosition='right';
 const text='老陈，借我五十，买包烟，明天还你。';
 const shot={shotId:'local-shop-dialogue-qa',duration:8,width:864,height:480,firstFrame,references:frame.plan.references,dialogueEvents:[{type:'speech',speakerId:frame.plan.references.find(r=>r.name==='孙嘉俊').assetId,speakerName:'孙嘉俊',delivery:'onscreen',text}],sourceExcerpt:'孙嘉俊：'+text,subtitle:'孙嘉俊：'+text,prompt:'integrated_multimodal_description: [Shot 1] One continuous eight-second medium two-shot at the school shop at night, starting exactly from the supplied image. Keep both faces and their distinct tracksuits unchanged. The boy on the viewer’s right, Sun Jiajun in dark blue sleeves, turns to the left boy and speaks the sole dialogue line, casually asking to borrow money. The boy on the left, Chen Shi in white sleeves, keeps his mouth closed throughout and listens reluctantly. Only after the right boy finishes speaking, the left boy takes a small banknote from his own pocket and extends it toward the right boy. The right boy receives it from the left boy. Never reverse the direction of this handover. Keep the same fixed camera and shop background.\n\noverall_soundscape: Quiet night ambience and the sole right-side speaker, no other voices or narration.\n\nnon_diegetic_music: N/A'};
 const response=await fetch('http://127.0.0.1:4173/api/film',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId:'AMS-LOCAL-SHOP-QA',title:'小卖部双人对白与递钱方向测试',shots:[shot]})}),out=await response.json();if(!response.ok)throw Error(out.error);fs.writeFileSync('test-artifacts/local-shop-video.json',JSON.stringify(out,null,2));console.log(out.run.id);
})().catch(e=>{console.error(e.message);process.exitCode=1});
